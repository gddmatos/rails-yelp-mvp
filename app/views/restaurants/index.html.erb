<h1>All Restaurants</h1>
<ul>
    <% @restaurants.each do |restaurant| %>
      <li>
        <%= restaurant.name %> <%= restaurant.category %> <%= link_to "details", restaurant_path(restaurant.id) %>
      </li>
    <% end %>
</ul>

<%= link_to "add a new restaurant", new_restaurant_path %>
